*** Begin Patch
*** Update File: src/gui/main_window.py
@@
-            processed_content = result.get('processed_text', '')
-            processing_time   = result.get('processing_time', 0)
-            reduction         = result.get('reduction_percentage', 0)
+            # Accept both dict (new/expected) and str (legacy backend) returns
+            try:
+                processed_content = result.get('processed_text', '')
+                processing_time   = float(result.get('processing_time', 0.0))
+                reduction         = float(result.get('reduction_percentage', 0.0))
+            except AttributeError:
+                # Backend returned a plain string; normalize it
+                processed_content = result if isinstance(result, str) else str(result)
+                processing_time = 0.0
+                # Compute a simple reduction metric vs. the input
+                src_len = len(source_content or "")
+                out_len = len(processed_content or "")
+                reduction = max(0.0, (1.0 - (out_len / src_len)) * 100.0) if src_len else 0.0
*** End Patch
